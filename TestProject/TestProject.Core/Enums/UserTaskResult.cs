using System;
using System.Collections.Generic;
using System.Text;

namespace TestProject.Core.Enum
{
    public enum UserTaskResult
    {
        Deleted = 0,
        Saved = 1,
        NotChanged = 2,
        Updated = 3
    }
}
